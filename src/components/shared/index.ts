/**
 * Shared components that can be imported across features.
 * These components act as bridges between feature-specific components
 * and the rest of the application, preventing direct cross-feature coupling.
 */

export { TaskEditModal, type TaskFormData } from './TaskEditModal';
